# x86-to-x64-dotnet
PowerShell script that updates a project’s Configuration Manager to target x64. Native DLL and COM dependencies are not automatically converted and must be handled manually; the script reports any such dependencies.
